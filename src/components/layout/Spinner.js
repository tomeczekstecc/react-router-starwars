import React from "react";
import spinner from "../../resources/spinner.gif";

const Spinner = () => {
  return (
    <div>
      <img src={spinner} alt=""/>
    </div>
  );
};

export default Spinner;
